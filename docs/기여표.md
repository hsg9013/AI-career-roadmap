# 개인 기여 입증 자료 — AI 진로 도우미 서비스 (황성권 · 장민 · 박도현)

## 0. 입증 요약 및 전제

| 구분 | 근거 |
|------|------|
| **Git 커밋 계정** | `hsg9013 <vrmaker65@gmail.com>` (공용 계정) — 저장소 39커밋 (`git shortlog -sne --all`) |
| **작업 브랜치** | `002-intent-specify` · `003-intent-specify` · `004-intent-specify` · `005-intent-specify` |
| **기능 단위 커밋** | 002(8) → 003(3) → 004(2) → 005(20+) 루프별 누적 구현 |
| **캔버스 기록** | DDBM ISP 캔버스(`abc.sumzip.com`) — `#60` 003 작업본 / `#85` 002 스냅샷 / `#87` 004 스냅샷. 산출물 `docs/ISP-AI진로로드맵.md`, 운용 스크립트 `scripts/ddbm-isp-*.mjs` |

> ⚠️ **전제(투명성):** 모든 커밋이 **공용 git 계정 하나로** push되어 있어, 커밋 작성자명만으로 3인을 구분할 수는 없다.
> 아래 **인별 분담은 팀 내부 작업 분담(자기보고)을 기능 도메인 기준으로 정리**한 것이며, 커밋 해시는 "해당 기능이 실제 구현·검증되었다"는 **공통 근거**로 제시한다.

---

## 1. 인별 담당 요약

| 담당자 | 담당 도메인 | 대표 기능요소 |
|--------|-------------|----------------|
| **황성권** | 학생 핵심 가치 + 기획(ISP) | 갭 진단, AI 진로 로드맵, ISP 캔버스 작성·운용 |
| **장민** | 멘토·외부 생태계 | 멘토 검수·하이브리드 피드백, 교육·활동 제휴사 포털, AI 데모 토글 |
| **박도현** | 수익화·품질/보안 | 역할별 SaaS 수익모델·결제, 세션·보안 안정화·테스트 |

---

## 2. 기여표 (기능요소 · 주담당 · 기여내용 · 근거)

| # | 기능요소 | 주담당 | 기여내용 (무엇을 / 어떻게) | 근거 (커밋 · 캔버스) |
|---|----------|--------|----------------------------|----------------------|
| 1 | **데이터 기반 갭 진단**(역량 부족 분석) | **황성권** | 목표 직무 요구역량 대비 보유역량을 점수화·시각화. 갭 0점 버그(활동 태그 미백필)를 태그 백필 시드 + 진단 스냅샷 시드로 해결, 온보딩에 역량 칩을 추가해 신규 가입자 점수에 즉시 반영 | 커밋 `bba7cac`, `0b262b7` · `backend/scripts/seed-activity-tags.ts` / 캔버스 "역량 분석·진단" 블록 (`docs/ISP-AI진로로드맵.md §1.3-1`) |
| 2 | **AI 진로 로드맵**(선배 코호트·근거) | **황성권** | 선배 합격 경로 k≥5 코호트 기반 추천, 표본 부족 시 직무 전체 폴백, 각 추천에 추천 이유(rationale) 부여. KPI 이벤트 발화·선배 정산 연동 보강 | 커밋 `0037295`, `7c6224c` · `backend/src/services/roadmap.ts` / 캔버스 "이행과제·로드맵" 블록 |
| 3 | **ISP 캔버스 작성·보완 + 루프 스냅샷 운용** | **황성권** | DDBM ISP 캔버스에 비전·4대 목표·갭분석·이행과제·로드맵 블록 작성/보완, 루프별(002/003/004) 캔버스 복제·동결 운용 스크립트 작성 | `docs/ISP-AI진로로드맵.md`, `scripts/ddbm-isp-clone.mjs`·`fill`·`rewrite`·`apply-003` / 캔버스 `#60`·`#85`·`#87` |
| 4 | **멘토(현직자) 검수·하이브리드 피드백** | **장민** | 제출물 상태머신(AI 1차→멘토 배정→완료 / 기한 초과 시 재배정·AI 폴백) 설계, 멘토 메인에 매핑 학생·출제 미션 현황 노출, 학생 화면 AI+멘토 조언 결합 조회 | 커밋 `620587f`, `d6a01d9`, `efc896b` · `backend/src/services/missions.ts`, `frontend-web/src/pages/Missions.vue` |
| 5 | **교육·활동 제휴사 포털 + 자체가입 승인 게이트** | **장민** | `edu_platform` 역할·로그인 SaaS 신설, 자체가입(가입대기·로그인 잠금) → 운영자 승인 게이트, 제휴사 소식(피드물)·광고 배너 발행·성과, 콘텐츠 삭제 | 커밋 `84a6aeb`, `df00d5b`, `9651c48` · `backend/src/modules/partners/router.ts`, `V029__add_edu_platform_role.sql` |
| 6 | **AI 데모 토글 안전장치** | **장민** | 플래그 파일로 재시작 없이 OAuth 실연동 on/off, 기본 OFF + 2시간 자동 만료로 시연 후 노출 방지 | 커밋 `d86ec45`, `c96638e` |
| 7 | **역할별 SaaS 수익모델·결제** | **박도현** | 4대 파트너 역할별 메인 완전 분기 + 맞춤 멤버십/플랜, 기업 무료/멤버십 2단계화·요금 현실화, 결제 상태머신(진행→완료/실패, 완료→취소/환불)과 멤버십 활성화 | 커밋 `fffb56d`, `916ac9c`, `d8df442` · `backend/src/services/payments.ts` |
| 8 | **세션·보안 안정화 + 품질 검증** | **박도현** | E2E로만 드러난 세션 버그 4건(refresh 토큰 회전 jti 충돌 등) 수정, 통합·E2E·성능 테스트(T039/T040/T043) 완비 | 커밋 `e211796` · `backend/src/005-roles.integration.test.ts`, `005-perf.test.ts`, `lib/jwt.ts` |

---

## 3. 근거 재현 방법 (검증용)

```bash
git shortlog -sne --all            # 커밋 계정 확인
git show 84a6aeb --stat            # 기능 단위 변경 파일 확인 (예: 제휴사 포털)
git log --oneline 005-intent-specify
less docs/ISP-AI진로로드맵.md       # 캔버스 작성 산출물
```
